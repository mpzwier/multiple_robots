/**/controller_manager:  #needs to have /**/ for the namespace of multiple robots 
  ros__parameters:
    update_rate: 200
    use_sim_time: true

    diff_drive_controller:
      type: differential_drive_controller/DifferentialDriveController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster


/**/diff_drive_controller:
  ros__parameters:
    left_wheel_names: ["left_wheel_joint"]
    right_wheel_names: ["right_wheel_joint"]

    wheel_separation: 0.2
    wheel_radius: 0.0381

    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0

    publish_limited_velocity: true

    publish_rate: 10.0
    odom_frame_id: odom
    base_frame_id: base_footprint
    
    tf_frame_prefix_enable: false #must be false to prevent double namespacing of the tf frames --> does not matter because tf from the controller go into a dummy value, ekf uses odom to publish tf's
    pose_covariance_diagonal : [0.001, 0.001, 0.001, 0.001, 0.001, 0.01]
    twist_covariance_diagonal: [0.001, 0.001, 0.001, 0.001, 0.001, 0.01]

    open_loop: true
    enable_odom_tf: true

    cmd_vel_timeout: 0.5
    #publish_limited_velocity: true
    #velocity_rolling_window_size: 10

    # Velocity and acceleration limits
    # Whenever a min_* is unspecified, default to -max_*
    linear.x.max_velocity: 5.0
    linear.x.min_velocity: -5.0
    linear.x.max_acceleration: 1.0

    angular.z.max_velocity: 1.0
    angular.z.min_velocity: -1.0
    angular.z.max_acceleration: 1.0
    angular.z.min_acceleration: -1.0

# Joint State Broadcaster
joint_state_broadcaster:
  ros__parameters:
    {}

    


